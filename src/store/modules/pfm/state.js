import { iconText, markConfig, help } from '../../../config/'
const state = {
    editorIsSplit: true,
    editorIsFullscrean: false,
    textareaContent: '',
    iconText: iconText,
    markConfig: markConfig,
    help: help
}
export default state
